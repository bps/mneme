[package]
name = "mneme"
version = "0.1.0"
edition = "2024"

[dependencies]
libc = "0.2.185"
rustix = { version = "1.1.4", features = ["pty", "termios", "process", "fs", "net", "event", "pipe"] }
signal-hook = "0.4.4"
